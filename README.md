backbone-require-rivets-todos
=============================

A demo of a Backbone application using RequireJS and Rivets

Documentation
-------------
You can view the annotated source by doing this:

    * `npm install -g grunt-cli`
    * `npm install`
    * `grunt`
    * open the `docs` directory
